export const useSwap = () => {
  const config = null;

  return config;
};

export const useWETHDeposit = () => {
  const config = null;

  return config;
};

export const useWETHWithdraw = () => {
  const config = null;

  return config;
};
